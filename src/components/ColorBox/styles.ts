import { Box, styled } from "@mui/material";

const ColorBoxContainer = styled(Box)`
  border-radius: 15px;
  padding: 1rem 2rem;
  width: 100%;
  display: grid;
  grid-template-columns: 1fr;
  place-items: center start;
`;

export default ColorBoxContainer;
